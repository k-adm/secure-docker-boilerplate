# Copy this file to .env and change the values
# cp .env.example .env

# --- PostgreSQL credentials ---
DB_USER=appuser
DB_PASS=change_me_in_production
DB_NAME=appdb

# --- Used by the app to connect to the database ---
DB_HOST=db
DB_PORT=5432
