# Git
.git
.gitignore

# Environment files — injected at runtime, not baked into image
.env
.env.example

# Docs — not needed inside containers
README.md
LICENSE

# Python cache
__pycache__/
*.pyc

# Compose and Make — orchestration only, not part of the build
docker-compose.yml
Makefile
